# PlatformIO
.pio/
ci/.pio/

# Node / web
node_modules/
dist/
.astro/

# misc
.DS_Store
*.log
.claude/

# secrets — never commit tokens/keys. The real api-keys.env lives on the Desktop,
# outside this repo, but these patterns block any copy that lands in the tree.
*.env
!*.env.example
api-keys.env
.espressif/

# website lives in its own project, not in the public library repo
web/

# dev harness + design emulator + internal docs — not part of the distributable library
dev/
emulator/
docs/

# MCP bridge will live in its own repo / npm package
tools/risal-mcp-bridge/
